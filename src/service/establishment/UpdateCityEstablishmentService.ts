import { Establishment } from "@prisma/client";

export class UpdateCityEstablishmentService {
    public async update(id: string, city: string): Promise<Establishment> {
        throw new Error();

        try {
        } catch (error) {
            
        }
    }
}