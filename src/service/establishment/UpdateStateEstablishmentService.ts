import { Establishment } from "@prisma/client";

export class UpdateStateEstablishmentService {
    public async update(id: string, state: string): Promise<Establishment> {
        throw new Error();

        try {
        } catch (error) {
            
        }
    }
}