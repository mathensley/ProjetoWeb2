import { Establishment } from "@prisma/client";

export class UpdateEmailEstablishmentService {
    public async update(id: string, email: string): Promise<Establishment> {
        throw new Error();

        try {
        } catch (error) {
            
        }
    }
}