import { Establishment } from "@prisma/client";

export class UpdateCEPEstablishmentService {
    public async update(id: string, cep: string): Promise<Establishment> {
        throw new Error();

        try {
        } catch (error) {
            
        }
    }
}